{
  "name": "realmforge-extractor",
  "version": "1.1.2",
  "private": true,
  "description": "Development tooling for RealmForge Extractor (build + tests). Players do not need Node.",
  "type": "module",
  "scripts": {
    "build": "node tools/build.mjs",
    "dist": "node tools/build.mjs --zip",
    "mock": "node tests/mock-server.mjs 3999",
    "test": "bash tests/run-tests.sh",
    "app": "bash tools/build-app.sh",
    "app:zip": "bash tools/build-app.sh && bash tools/pack-app.sh"
  },
  "devDependencies": {
    "tree-sitter-powershell": "^0.26.4",
    "web-tree-sitter": "^0.27.0"
  }
}
